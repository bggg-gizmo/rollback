<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="group.backgroundgremlin.rollbacker" android:versionCode="2" android:versionName="1.0.1">
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="36" />

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".RollbackerApp"
        android:allowBackup="false"
        android:description="@string/app_description"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <meta-data android:name="group.backgroundgremlin.rollbacker.DEVELOPER" android:value="@string/developer_name" />
        <meta-data android:name="group.backgroundgremlin.rollbacker.PUBLISHER" android:value="@string/publisher_name" />
        <meta-data android:name="group.backgroundgremlin.rollbacker.TAGLINE" android:value="@string/tagline" />
        <meta-data android:name="group.backgroundgremlin.rollbacker.COPYRIGHT" android:value="@string/copyright_notice" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SnapshotJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".SnapshotSweepJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".PackageChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".InstallStatusReceiver"
            android:exported="false" />
    </application>
</manifest>
